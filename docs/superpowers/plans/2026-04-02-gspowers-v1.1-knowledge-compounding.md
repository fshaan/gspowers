# gspowers v1.1 Knowledge Compounding Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add knowledge compounding loop to gspowers — compound step for experience capture, pre-brainstorming knowledge search, and simplified init for existing projects.

**Architecture:** Minimal changes to 6 existing markdown skill files. No new files created. Each task modifies exactly one file with precise insertions/replacements.

**Tech Stack:** Claude Code skills (markdown), Compound Engineering plugin (`ce:compound`, `learnings-researcher` agent)

**Pre-implementation:** Before starting, verify `ce:compound` output format by reading `~/.claude/plugins/cache/compound-engineering-plugin/compound-engineering/*/skills/ce-compound/SKILL.md` to confirm it writes to `docs/solutions/` with YAML frontmatter (eng review decision #5).

---

### Task 1: Update SKILL.md — step mapping, flow map, description

**Files:**
- Modify: `~/.claude/skills/gspowers/SKILL.md`

- [ ] **Step 1: Update the description in frontmatter**

In the YAML frontmatter `description` field, change:

```yaml
description: |
  AI 开发全流程编排器。协调 gstack（产品/架构）和 superpowers（工程/测试）。
  运行 /gspowers 查看当前进度和下一步操作。
```

to:

```yaml
description: |
  AI 开发全流程编排器。协调 gstack（产品/架构）和 superpowers（工程/测试）。
  运行 /gspowers 查看当前进度和下一步操作。可生成脚手架文件（如 Update_Plan.md）。
```

- [ ] **Step 2: Update the step progression mapping**

Replace the entire step mapping code block:

```
office-hours → plan-ceo-review（若跳过则 plan-eng-review）  [plan]
plan-ceo-review → plan-eng-review                           [plan]
plan-eng-review → brainstorming                             [execute]
brainstorming → writing-plans（若两个 artifact 都有则 subagent-dev）[execute]
writing-plans → subagent-dev                                [execute]
subagent-dev → review                                       [finish]
review → qa（若 has_ui）或 ship                              [finish]
qa → ship                                                   [finish]
ship → document-release                                     [finish]
document-release → （current_phase 设为 done）               [done]
```

with:

```
office-hours → plan-ceo-review（若跳过则 plan-eng-review）  [plan]
plan-ceo-review → plan-eng-review                           [plan]
plan-eng-review → brainstorming                             [execute]
brainstorming → writing-plans（若两个 artifact 都有则 subagent-dev）[execute]
writing-plans → subagent-dev                                [execute]
subagent-dev → review                                       [finish]
review → qa（若 has_ui）或 ship                              [finish]
qa → ship                                                   [finish]
ship → compound                                             [finish]
compound → document-release                                 [finish]
document-release → （current_phase 设为 done）               [done]
```

- [ ] **Step 3: Update the flow map**

Replace the flow map template:

```
★ 规划期 ──────────────────────────
├─ [{status}] office-hours
├─ [{status}] plan-ceo-review (可选)
├─ [{status}] plan-eng-review
★ 执行期 ──────────── /clear ────
├─ [{status}] brainstorming
├─ [{status}] writing-plans
├─ [{status}] subagent-dev
★ 收尾期 ──────────── /clear ────
├─ [{status}] /review
├─ [{status}] /qa (条件: has_ui)
├─ [{status}] /ship
└─ [{status}] /document-release
```

with:

```
★ 规划期 ──────────────────────────
├─ [{status}] office-hours
├─ [{status}] plan-ceo-review (可选)
├─ [{status}] plan-eng-review
★ 执行期 ──────────── /clear ────
├─ [{status}] brainstorming
├─ [{status}] writing-plans
├─ [{status}] subagent-dev
★ 收尾期 ──────────── /clear ────
├─ [{status}] /review
├─ [{status}] /qa (条件: has_ui)
├─ [{status}] /ship
├─ [{status}] /ce:compound (可选)
└─ [{status}] /document-release
```

- [ ] **Step 4: Commit**

```bash
git add ~/.claude/skills/gspowers/SKILL.md
git commit -m "feat: add compound step to gspowers flow map and step mapping"
```

---

### Task 2: Add compound section to finish.md

**Files:**
- Modify: `~/.claude/skills/gspowers/references/finish.md`

- [ ] **Step 1: Update the ship section's next step**

In the `## ship` section, change step 4:

```markdown
4. 设 `current_step` 为 `"document-release"`
```

to:

```markdown
4. 设 `current_step` 为 `"compound"`
```

- [ ] **Step 2: Insert the compound section**

Insert the following new section between `## ship` and `## document-release`:

```markdown
---

## compound

> 可选步骤。将本次开发经验沉淀到 `docs/solutions/`，形成知识复利。

1. **可用性检查**：检查当前会话的可用 skills 列表中是否包含 `compound-engineering:ce-compound`（即 `/ce:compound`）
   - 不可用 → 显示："ce:compound 不可用，跳过经验沉淀"
   - 将 `"compound"` 加入 `skipped_steps`
   - 设 `current_step` 为 `"document-release"`
   - 更新 state.json + 重写 handoff.md
   - 跳到 document-release

2. **引导式提问**（帮助用户判断是否有经验值得记录）：

   > "在回答之前回顾一下本次开发过程：
   > - 遇到了意外的坑或 debug 难题吗？
   > - 发现了什么新的最佳实践或更好的做法？
   > - 有什么下次想避免或改进的？"

3. **根据回答**：
   - 全部"否" → 将 `"compound"` 加入 `skipped_steps`，设 `current_step` 为 `"document-release"`，更新 state.json + 重写 handoff.md，跳到 document-release
   - 任一"是" → 提示用户执行：

   ```
   /ce:compound
   ```

4. **用户完成后**：
   1. 将 `"compound"` 加入 `completed_steps`
   2. 设 `current_step` 为 `"document-release"`
   3. 更新 state.json + 重写 handoff.md
```

- [ ] **Step 3: Commit**

```bash
git add ~/.claude/skills/gspowers/references/finish.md
git commit -m "feat: add compound step to finish phase (ship → compound → document-release)"
```

---

### Task 3: Add knowledge search to execute.md

**Files:**
- Modify: `~/.claude/skills/gspowers/references/execute.md`

- [ ] **Step 1: Insert knowledge search logic before brainstorming prompt**

In the `## brainstorming` section, replace:

```markdown
提示用户执行：

\```
/brainstorm

请读取 .gspowers/artifacts/architecture.md 作为架构输入。
\```
```

with:

```markdown
### 前置知识搜索

在提示 brainstorming 之前，自动搜索历史经验（导航员模式的唯一例外：只读预处理）。

1. 用 Bash 检查：`test -d docs/solutions/ && echo exists || echo missing`
   - **missing** → 显示："📚 未找到历史经验库（docs/solutions/），从零开始 brainstorming"，跳到步骤 3

2. **exists** → 用 Agent 工具派发 `learnings-researcher` agent（`subagent_type` 设为 `compound-engineering:research:learnings-researcher`）：
   - 先用 Read 工具读取项目输入文件（existing 项目读 `Update_Plan.md`，new 项目读 `.gspowers/artifacts/product-requirements.md`），提取项目主题摘要
   - agent prompt：`"搜索 docs/solutions/ 中与以下主题相关的历史经验：{项目主题摘要}。优先匹配 YAML frontmatter 中的 tags 和 problem 字段，其次全文。返回最相关的 ≤3 条结果，每条包含文件路径和关键要点摘要。"`
   - **有结果** → 按以下格式写入 `.gspowers/artifacts/prior-learnings.md`：
     ```markdown
     ## 相关历史经验 (From docs/solutions/)

     - **[文件名]**: [核心要点摘要]
       - **关联风险**: [曾经踩过的坑]
       - **建议做法**: [本次可复用的实践]
     ```
     在 state.json `artifacts` 中记录 `"prior-learnings": ".gspowers/artifacts/prior-learnings.md"`
     显示："📚 找到 {N} 条相关历史经验，已写入 prior-learnings.md"
   - **无结果** → 显示："📚 未找到相关历史经验，从零开始 brainstorming"，不生成 prior-learnings.md
   - **agent 派发失败** → 静默降级，等同无结果

3. 提示用户执行：

   有 `prior-learnings.md` 时：
   \```
   /brainstorm

   请读取以下文件作为输入：
   - .gspowers/artifacts/architecture.md（架构）
   - .gspowers/artifacts/prior-learnings.md（历史经验）
   \```

   无 `prior-learnings.md` 时：
   \```
   /brainstorm

   请读取 .gspowers/artifacts/architecture.md 作为架构输入。
   \```
```

- [ ] **Step 2: Commit**

```bash
git add ~/.claude/skills/gspowers/references/execute.md
git commit -m "feat: add pre-brainstorming knowledge search via learnings-researcher"
```

---

### Task 4: Simplify init flow for existing projects

**Files:**
- Modify: `~/.claude/skills/gspowers/references/init.md`

- [ ] **Step 1: Replace the existing project init flow**

In `## 2. 收集项目信息`, under `**Q1**`, replace the existing project handling:

```markdown
- 已有项目 → `project_type = "existing"`
  - 告诉用户需要在项目根目录创建 `Update_Plan.md`，给出模板（见下方）
  - 自动设置 `mode = "quick"`，跳过 Q2
```

with:

```markdown
- 已有项目 → `project_type = "existing"`
  - 用 Bash 检查：`test -f Update_Plan.md && echo exists || echo missing`
  - **exists** → 显示 "检测到已有 Update_Plan.md，使用现有文件继续"
  - **missing** → 执行自动生成流程（见下方「Update_Plan.md 自动生成」）
  - 自动设置 `mode = "quick"`，跳过 Q2
```

- [ ] **Step 2: Replace the Update_Plan.md template section**

Replace the entire `## Update_Plan.md 模板` section at the bottom with:

```markdown
## Update_Plan.md 自动生成

当 existing 项目没有 Update_Plan.md 时，执行以下流程：

1. 询问用户：
   > "请用自然语言描述你这次要做什么更新。
   > 比如：修复什么问题、添加什么功能、优化什么流程。
   > 尽量说清楚问题现状、期望结果和约束条件。"

2. 根据用户描述，生成 Update_Plan.md 内容，填充 4 个字段（问题现状、期望结果、约束条件、影响范围）。文件头部加 `<!-- generated-by: gspowers-v1.1 -->` 标记。模板结构：

   ```markdown
   <!-- generated-by: gspowers-v1.1 -->
   # 更新计划

   ## 问题现状
   {根据用户描述填充}

   ## 期望结果
   {根据用户描述填充}

   ## 约束条件
   {根据用户描述填充，若未提及则写"用户未指定，请在 plan-eng-review 中确认"}

   ## 影响范围
   {根据用户描述填充}
   ```

3. 展示生成的完整内容给用户确认：
   > "以下是根据你的描述生成的更新计划，请确认或修改：
   > [展示完整内容]
   > 确认无误？（是 / 需要修改）"

4. 用户确认 → 用 Write 工具写入 `Update_Plan.md`
   用户要修改 → 根据反馈调整后重新展示，直到确认

5. 继续 Q3（has_ui 询问）
```

- [ ] **Step 3: Commit**

```bash
git add ~/.claude/skills/gspowers/references/init.md
git commit -m "feat: auto-generate Update_Plan.md from natural language for existing projects"
```

---

### Task 5: Update sop.md — flow table and knowledge compounding section

**Files:**
- Modify: `~/.claude/skills/gspowers/references/sop.md`

- [ ] **Step 1: Update the full flow table**

In `## 完整流程（新项目 + 完整模式）`, replace the table:

```markdown
| # | 步骤 | 工具 | 命令 | 产出物 | /clear |
|---|------|------|------|--------|--------|
| 1 | office-hours | gstack | `/office-hours` | product-requirements.md | 否 |
| 2 | plan-ceo-review | gstack | `/plan-ceo-review` | ceo-review.md | 否 |
| 3 | plan-eng-review | gstack | `/plan-eng-review` | architecture.md | **是** |
| 4 | brainstorming | superpowers | `/brainstorm` | design-spec.md | 否 |
| 5 | writing-plans | superpowers | 自动衔接 | implementation-plan.md | 否 |
| 6 | subagent-dev | superpowers | 子代理执行 | 代码 + commits | **是** |
| 7 | /review | gstack | `/review` | review-report.md | 否 |
| 8 | /qa | gstack | `/qa http://localhost:PORT` | QA 修复 | 否 |
| 9 | /ship | gstack | `/ship` | PR 链接 | 否 |
| 10 | /document-release | gstack | `/document-release` | 更新文档 | 否 |
```

with:

```markdown
| # | 步骤 | 工具 | 命令 | 产出物 | /clear |
|---|------|------|------|--------|--------|
| 1 | office-hours | gstack | `/office-hours` | product-requirements.md | 否 |
| 2 | plan-ceo-review | gstack | `/plan-ceo-review` | ceo-review.md | 否 |
| 3 | plan-eng-review | gstack | `/plan-eng-review` | architecture.md | **是** |
| 4 | brainstorming | superpowers | `/brainstorm` | design-spec.md | 否 |
| 5 | writing-plans | superpowers | 自动衔接 | implementation-plan.md | 否 |
| 6 | subagent-dev | superpowers | 子代理执行 | 代码 + commits | **是** |
| 7 | /review | gstack | `/review` | review-report.md | 否 |
| 8 | /qa | gstack | `/qa http://localhost:PORT` | QA 修复 | 否 |
| 9 | /ship | gstack | `/ship` | PR 链接 | 否 |
| 10 | /ce:compound (可选) | CE | `/ce:compound` | docs/solutions/*.md | 否 |
| 11 | /document-release | gstack | `/document-release` | 更新文档 | 否 |
```

- [ ] **Step 2: Add knowledge compounding section**

After the `## 快速模式` section, add:

```markdown
## 知识复利

gspowers v1.1 引入知识复利闭环：

1. **compound 步骤**（ship 后）：调用 `/ce:compound` 将本次开发经验结构化记录到 `docs/solutions/`
2. **知识搜索**（brainstorming 前）：gspowers 自动搜索 `docs/solutions/` 中的相关历史经验，写入 `prior-learnings.md` 作为 brainstorming 参考

闭环：`compound → docs/solutions/ → 知识搜索 → brainstorming`
```

- [ ] **Step 3: Update the existing project flow section**

In `## 精简流程（已有项目）`, replace:

```markdown
跳过 office-hours 和 plan-ceo-review，从 plan-eng-review 开始。需要在项目根目录创建 `Update_Plan.md`。
```

with:

```markdown
跳过 office-hours 和 plan-ceo-review，从 plan-eng-review 开始。gspowers 会自动引导生成 `Update_Plan.md`（也支持使用已有的）。
```

- [ ] **Step 4: Commit**

```bash
git add ~/.claude/skills/gspowers/references/sop.md
git commit -m "docs: update SOP with compound step and knowledge compounding section"
```

---

### Task 6: Add /ce:compound entry to tools-guide.md

**Files:**
- Modify: `~/.claude/skills/gspowers/references/tools-guide.md`

- [ ] **Step 1: Add ce:compound entry**

At the end of the file, append:

```markdown
---

## /ce:compound — 经验沉淀

**何时用：** 每次 /ship 后，有值得记录的经验时。

**时机：** ship 之后、document-release 之前。已整合为 gspowers 主流程中的可选步骤。

**命令：**
```
/ce:compound
```

将本次开发中的问题解决方案结构化记录到 `docs/solutions/`，带 YAML frontmatter 便于未来搜索。gspowers 在后续项目的 brainstorming 前会自动搜索这些经验。

---

## /ce:compound-refresh — 经验刷新

**何时用：** 重构、迁移、依赖升级后，检查历史经验是否过时。

**时机：** 周期性执行，不在单次开发流中。

**命令：**
```
/ce:compound-refresh
```

审查 `docs/solutions/` 中的文档，更新、合并或标记过时的经验记录。
```

- [ ] **Step 2: Commit**

```bash
git add ~/.claude/skills/gspowers/references/tools-guide.md
git commit -m "docs: add ce:compound and ce:compound-refresh to tools guide"
```

---

### Task 7: End-to-end verification

- [ ] **Step 1: Verify all 6 files were modified correctly**

Read each modified file and verify:
1. `SKILL.md` — step mapping has `ship → compound`, flow map has `/ce:compound (可选)`, description has "可生成脚手架文件"
2. `finish.md` — compound section exists between ship and document-release, ship's next step is `"compound"`
3. `execute.md` — knowledge search logic exists before brainstorming prompt
4. `init.md` — existing project flow has auto-generation, Update_Plan.md template section replaced
5. `sop.md` — flow table has 11 rows with compound at #10, knowledge compounding section exists
6. `tools-guide.md` — `/ce:compound` and `/ce:compound-refresh` entries exist

- [ ] **Step 2: Verify internal consistency**

Check that all step mapping references are consistent across files:
- `ship → compound → document-release` appears consistently in SKILL.md, finish.md, sop.md
- The `learnings-researcher` agent name `compound-engineering:research:learnings-researcher` matches the actual CE plugin agent path
- The skill name `compound-engineering:ce-compound` matches the actual CE plugin skill

- [ ] **Step 3: Final commit (if any fixes needed)**

```bash
git add -A
git commit -m "fix: address verification findings in gspowers v1.1 files"
```
